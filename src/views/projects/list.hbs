<br><br><br><div class="container p-4">
    <div class="row">
        {{#each projects}}
            <div class="col-md-3">
                <div class="card text-center">
                    <div class="card-body">
                        <img src="{{imagen}}" height="50%" width="50%">
                        <h3 class="card-title text-uppercase">
                            {{nombre}}
                        </h3>
                        <p class="m-2">{{descripcion}}</p>
                        <a href="/portfolio/delete/{{id}}" class="btn btn-outline-danger">Borrar</a>
                        <a href="/portfolio/edit/{{id}}" class="btn btn-outline-info">Editar</a>
                    </div>
                </div> 
                <br>
            </div>
            {{else}}
            <div class="col-md-4 mx-auto">
                <div class="card card-body text-center">
                    <p>Aún no hay ningun proyecto agregado</p>
                    <a href="/portfolio/add">Agrega alguno ahora</a>
                </div>
            </div>
        {{/each}}
    </div>
</div>